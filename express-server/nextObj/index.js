const { middleware1 } = require("./middleware1");
const { middleware2 } = require("./middleware2");
const { nextObjController } = require("./nextObjController");

module.exports = { middleware1, middleware2, nextObjController };
